<UserControl x:Name="PopupWindowControl" x:Class="TemperatureIconMeterWPF.PopupWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TemperatureIconMeterWPF"
             mc:Ignorable="d" 
        xmlns:properties="clr-namespace:TemperatureIconMeterWPF.Properties"
             d:DesignHeight="350" d:DesignWidth="200" Loaded="PopupWindowControl_Loaded" Unloaded="PopupWindowControl_Unloaded">
    <Border BorderThickness="1" CornerRadius="8" BorderBrush="DarkGray" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
        <Grid Width="220" Margin="8,0,8,8">
            <ItemsControl ItemsSource="{Binding TemperatureMeter.HardwareTreeNodes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <TextBlock  HorizontalAlignment="Center" Margin="0,4,0,4" FontWeight="Bold" Foreground="RoyalBlue" 
                                TextWrapping="WrapWithOverflow" Text="{Binding Name}"/>
                            <ItemsControl ItemsSource="{Binding Sensors}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Path=DisplayName}" HorizontalAlignment="Left">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSelected}" Value="true">
                                                                <Setter Property="Foreground" Value="DarkGreen"/>
                                                                <Setter Property="FontWeight" Value="Bold"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding ConnectionStatus}" Value="false">
                                                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <TextBlock Grid.Column="1" TextAlignment="Right" Foreground="DarkRed">
                                                <TextBlock.Text>
                                                    <Binding Path="Max" StringFormat="{}▲{0}"/>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <TextBlock Grid.Column="2" TextAlignment="Right" Foreground="RoyalBlue" Margin="4,0,0,0">
                                                <TextBlock.Text>
                                                    <Binding Path="Min" StringFormat="{}▼{0}"/>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <TextBlock Grid.Column="3" TextAlignment="Right" FontWeight="Bold"  Margin="8,0,0,0">
                                                <TextBlock.Text>
                                                    <Binding Path="Value" StringFormat="{}{0}°C"/>
                                                </TextBlock.Text>
                                            </TextBlock>                                            
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Image x:Name="ImageClose" Source="images\cross.png" Stretch="None" 
                   HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,4,0,0"
                   MouseUp="ImageClose_MouseUp" TouchUp="ImageClose_TouchUp" />
        </Grid>
    </Border>
</UserControl>
